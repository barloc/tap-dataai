version: 1
send_anonymous_usage_stats: true
project_id: "tap-dataai"
plugins:
  extractors:
  - name: "tap-dataai"
    namespace: "tap_dataai"
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
